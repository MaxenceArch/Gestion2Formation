<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Formations</title>
    <style>
        /* Styles pour la barre de navigation */
        .navbar {
            background: linear-gradient(to right, #8e44ad, #c0392b); /* Dégradé de couleurs */
            padding: 10px 20px;
            border-radius: 10px; /* Coins arrondis */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            display: flex;
            justify-content: flex-end;
        }

        /* Styles pour les liens dans la barre de navigation */
        .navbar a {
            color: #fff;
            padding: 10px 20px;
            border-radius: 4px; /* Coins arrondis */
            text-decoration: none;
            transition: background-color 0.3s ease; /* Animation de transition */
        }

        /* Animation au survol des liens */
        .navbar a:hover {
            background-color: rgba(255, 255, 255, 0.1); /* Légère opacité au survol */
        }

        /* Styles personnalisés pour l'affichage des formations */
        .formation-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 20px;
            border: 1px solid #ddd;
            margin-bottom: 20px;
            background-color: #f9f9f9;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        /* Animation au survol des conteneurs de formation */
        .formation-container:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        /* Styles pour les détails de la formation */
        .formation-details {
            flex-grow: 1;
        }

        .formation-details b {
            color: #333;
        }

        /* Styles pour les boutons de la formation */
        .formation-buttons {
            display: flex;
            align-items: center;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-left: 10px;
        }

        /* Animation au survol des boutons */
        .btn:hover {
            transform: translateY(-2px);
        }

        /* Styles pour les boutons "danger" */
        .btn-danger {
            background-color: #FF3F3F;
            color: #fff;
        }

        /* Styles pour les boutons "primary" */
        .btn-primary {
            background-color: #3F7CFF;
            color: #fff;
        }

        /* Styles pour les boutons "success" */
        .btn-success {
            background-color: #28a745;
            color: #fff;
        }

        /* Styles pour les boutons "info" */
        .btn-info {
            background-color: #17a2b8;
            color: #fff;
        }

        /* Style pour le message d'alerte */
        .alert {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 4px;
            background-color: #17a2b8;
            color: #fff;
        }
    </style>
</head>
<body>
    <!-- Contenu de la page -->
</body>
</html>
        
</head>
<body>
    <nav class="navbar">
        <a href="{{ path('app_formation_ajouter') }}">Ajouter une formation</a>
        <a href="{{ path('app_affProduit') }}">Afficher les produits</a>
        <a href="{{ path('app_aff') }}" >Afficher les formations</a>
        <a href="{{ path('app_affEmploye') }}">Afficher les employés</a>
        <a href="{{ path('app_liste_inscription_admin') }}" >Inscriptions en attente</a>
        <a href="{{ path('app_liste_inscription_accepte') }}">Inscriptions Acceptée</a>
        <a href="{{ path('app_liste_inscription_refuse') }}">Inscriptions Refusée</a>
        <a href="{{ path('app_login') }}">Se déconnecter</a>
    </nav>

    {% if message %}
        <div class="alert alert-info">{{ message }}</div>
    {% endif %}

    {% for formation in ensFormation %}
        <div class="formation-container">
            <div class="formation-details">
                <b>Date de début:</b> {{ formation.dateDebut|date("m/d/Y") }}<br/>
                <b>Nombre d'heures:</b> {{ formation.nbrHeure }}<br/>
                <b>Département:</b> {{ formation.departement }}<br/>
                <b>Produit:</b> {{ formation.produit.libelle }}<br/>
                <b>Description: </b>{{formation.description}}</br>
            </div>
            <div class="formation-buttons">
                <a href="{{ path('app_formation_sup', {'id': formation.id}) }}" class="btn btn-danger">Supprimer</a>
                <a href="{{ path('app_formation_modifier', {'id': formation.id}) }}" class="btn btn-primary">Modifier</a>
            </div>
        </div>
    {% endfor %}
</body>
</html>
